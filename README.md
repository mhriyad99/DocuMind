# DocuMind

### Project Setup:
Install uv python library first.
 ```
 pip install uv
 uv sync
 ```
Set environment variables:
```angular2html
set -a            # Automatically export all subsequent variables
source .env       # Read the variables from your file
set +a            # Disable automatic exporting
```
This sets the env's for the that terminal session only.